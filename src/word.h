#pragma once

#include <array>
#include <string>
#include <vector>

namespace wordle {

class SearchSpace {
public:

private:
    std::vector<std::string> possible_wordles_{};
};

} // namespace wordle